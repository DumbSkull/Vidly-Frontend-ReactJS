import React, { Component } from "react";

class Customers extends Component {
  state = {};
  render() {
    return (
      <React.Fragment>
        <section class="jumbotron text-center">
          <h1>Customers</h1>
          <p className="lead text-muted">*Page still under construction* </p>
        </section>
      </React.Fragment>
    );
  }
}

export default Customers;
